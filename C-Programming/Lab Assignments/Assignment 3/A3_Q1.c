#include <stdio.h>
int main()
{
    int n;   //Declaration of a variable.
    printf("Enter the value of the variable : ");     //taking a value for the variable from user.
    scanf("%d",&n);                               //Putting the user input value in a variable.
    printf("Value of the variable is......%d",n);     //Printing the user input value.
    return 0;
}